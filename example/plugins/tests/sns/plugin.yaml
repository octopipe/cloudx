kind: Plugin
metadata:
  name: sns
spec:
  inputs:
    - label: Name
      name: name
      type: text
      help: the name of deployment
      required: true
    - label: Region
      name: region
      type: text
      help: the region of sns
      required: true

